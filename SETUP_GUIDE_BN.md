# ⚙️ টেকনিক্যাল ডিপ-ডাইভ: ইমপ্লিমেন্টেশন গাইড

এই গাইডটি **রায়ান এআই প্রোডাক্ট সেলস ম্যানেজার**-এর অ্যাডভান্সড কনফিগারেশন, নিরাপত্তার সর্বোত্তম অনুশীলন এবং সিস্টেমের সূক্ষ্ম টিউনিং কভার করে।

---

## 🏗 সাব-নোড আর্কিটেকচার ওভারভিউ
প্রোফেশনাল মান বজায় রাখার জন্য নিচের নোডগুলো বর্ণিত পদ্ধতিতে কনফিগার করা জরুরি:

### ১. ওমনি-চ্যানেল প্রসেসর (The Switchboard)
*   **উদ্দেশ্য:** ফেসবুক, ইনস্টাগ্রাম এবং হোয়াটসঅ্যাপ থেকে আসা ডেটাকে একটি নির্দিষ্ট ফরম্যাটে রূপান্তর করা।
*   **ফরম্যাট:** `{ platform: string, senderId: string, senderName: string, message: string }`।
*   **টিউনিং:** ইনস্টাগ্রাম ব্যবহার করলে আপনার মেটা অ্যাপে "Messaging" পারমিশন চালু আছে কিনা নিশ্চিত করুন।

### ২. গুগল জেমিনি এআই নোড (The Brain)
*   **মডেল:** গতির জন্য `gemini-1.5-flash` অথবা জটিল যুক্তির জন্য `gemini-1.5-pro` ব্যবহার করুন।
*   **টেম্পারেচার (Temperature):** `0.7` সেট করুন। (এটি এআই-কে মানুষের মতো বৈচিত্র্যময় কথা বলতে সাহায্য করে এবং উত্তরের নির্ভুলতা বজায় রাখে)।
*   **সিস্টেম প্রম্পট:** এতে সবসময় "অপারেশনাল লিমিট" উল্লেখ করুন (যেমন- "অ্যাডমিনের অনুমতি ছাড়া ৫%-এর বেশি ডিসকাউন্ট দেবেন না")।

### ৩. টুল ইনভেন্টরি (The Hands)
*   **Get Catalog:** এটি `Read` পারমিশনসহ কনফিগার করতে হবে।
*   **Add Order/Customer:** এটি `Append` পারমিশনসহ কনফিগার করতে হবে।
*   **Update/Cancel:** এটি `Order ID`-কে ইউনিক কি (Key) হিসেবে ব্যবহার করে `Update` পারমিশনসহ সেট করতে হবে।

---

## 🚀 অ্যাডভান্সড সেটআপ ধাপসমূহ

### 🔐 নিরাপত্তা এবং অ্যাক্সেস কন্ট্রোল
১. **n8n এনক্রিপশন:** ডিফল্টভাবে চালু থাকে। তবে আপনার n8n ফাইলটি অন্যদের দেওয়ার আগে ক্রেডেনশিয়ালগুলো রিমুভ করে নিন।
২. **এপিআই কি ম্যানেজমেন্ট:** 
    *   আপনার গুগল এপিআই কি প্রতি ৯০ দিন পরপর পরিবর্তন (Rotate) করুন।
    *   ২৪ ঘণ্টার টেম্পোরারি টোকেনের বদলে **Meta Permanent System User Token** ব্যবহার করুন।
৩. **গুগল শিট প্রটেকশন:** শিটের প্রথম সারি (Headers) এবং `Config` শিট লক করে রাখুন যাতে শুধু অ্যাডমিন এডিট করতে পারে।

### 📊 প্রফেশনাল ডেটা স্ট্রাকচারিং
আপনার গুগল শিটের হেডারগুলো হুবহু নিচের মতো হওয়া উচিত:
*   **Customers:** `customer_id`, `whatsapp_number`, `name`, `address`, `email`.
*   **Orders:** `order_id`, `customer_id`, `whatsapp_number`, `product_name`, `quantity`, `status`.
*   **Config:** `product_name`, `price`, `description`, `stock_status`.

### 🌐 ওয়েব হুক ভেরিফিকেশন (Meta Portals)
*   **ভেরিফিকেশন টোকেন:** একটি র্যান্ডম স্ট্রিং তৈরি করুন (যেমন- `Rayyan_AI_2026`)।
*   **ভেরিফিকেশন ইউআরএল:** n8n-এর প্রোডাকশন ইউআরএল (Production URL) ব্যবহার করুন।
*   **সাবস্ক্রিপশন:** মেটা ওয়েব হুক সেটিংসে অবশ্যই `messages` ফিল্ডটি সাবস্ক্রাইব করুন।

---

## 📈 স্কেল করার নির্দেশিকা
আপনার ব্যবসা বড় হওয়ার সাথে সাথে:
১. **SQL-এ স্থানান্তর:** অর্ডার ১০,০০০ এর বেশি হলে গুগল শিটের বদলে PostgreSQL বা MySQL ব্যবহার করুন।
২. **মিডিয়া সাপোর্ট:** ক্যাটালগ থেকে ছবির ইউআরএল নিয়ে হোয়াটসঅ্যাপে "Image" মেসেজ পাঠানোর ব্যবস্থা করুন।
৩. **হিউম্যান হ্যান্ডওভার:** যদি এআই কোনো সমস্যার সমাধান করতে না পারে, তবে অ্যাডমিনকে ইমেল বা মেসেজ দেওয়ার লজিক যোগ করুন।

---

## 🆘 গুরুত্বপূর্ণ সমস্যার সমাধান
১. **"এআই উত্তর দিচ্ছে না":** সাধারণত গুগল ক্লাউড এপিআই লিমিট শেষ হলে বা সিস্টেম প্রম্পট খালি থাকলে এমন হয়।
২. **"অর্ডার সেভ হচ্ছে না":** গুগল শিটের সেল লিমিট (১০ মিলিয়ন) অতিক্রম করেছে কিনা চেক করুন।
৩. **"ভুল মুদ্রার নাম (Currency)":** এআই অনেক সময় নিজের মতো কারেন্সি ধরে নেয়। তাই সিস্টেম মেসেজে স্পষ্টভাবে `টাকা` বা `BDT` লিখে দিন।

---
**প্রযুক্তিগত সহায়তা:**
রায়ান কোয়ান্টাম ল্যাবস ইন্টিগ্রেশন টিম।  
*হাই-পারফরম্যান্স এজেন্টিক ওয়ার্কফ্লোতে বিশেষজ্ঞ।*
